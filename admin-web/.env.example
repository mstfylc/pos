# Uyanık Admin Web — ortam değişkenleri örneği
# Gerçek .env commit EDİLMEZ (kök .gitignore secret kuralları).

# Backend API taban adresi (openapi client bu adrese bağlanır — sonraki paket)
VITE_API_BASE_URL=http://localhost:5080
